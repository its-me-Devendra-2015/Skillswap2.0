// SkillSwap v6 — v5 visual system preserved
const KEY='skillswap_multipage_v6';
const LEGACY_KEYS=['skillswap_multipage_v5','skillswap_v3'];
const defaults={users:[],current:null,profiles:{},skills:[
{id:'s1',name:'Photoshop',icon:'🎨',category:'Design',description:'Photo editing, retouching and creative compositing.',teacherIds:[]},
{id:'s2',name:'Web Development',icon:'💻',category:'Technology',description:'Build responsive websites with HTML, CSS and JavaScript.',teacherIds:[]},
{id:'s3',name:'Python',icon:'🐍',category:'Programming',description:'Programming, automation and problem solving.',teacherIds:[]},
{id:'s4',name:'Video Editing',icon:'🎬',category:'Creative',description:'Cut videos, build transitions and tell better stories.',teacherIds:[]},
{id:'s5',name:'Photography',icon:'📷',category:'Creative',description:'Composition, lighting and camera fundamentals.',teacherIds:[]},
{id:'s6',name:'Drawing',icon:'✏️',category:'Art',description:'Sketching, digital art and visual storytelling.',teacherIds:[]},
{id:'s7',name:'Public Speaking',icon:'🎤',category:'Communication',description:'Present ideas clearly and confidently.',teacherIds:[]},
{id:'s8',name:'3D Modelling',icon:'🧊',category:'Design',description:'Create models, scenes and visual assets.',teacherIds:[]},
{id:'s9',name:'Cricket',icon:'🏏',category:'Sports',description:'Batting, bowling, fielding and match awareness.',teacherIds:[]},
{id:'s10',name:'Music',icon:'🎵',category:'Creative',description:'Explore instruments, rhythm and music production.',teacherIds:[]},
{id:'s11',name:'Graphic Design',icon:'🖌️',category:'Design',description:'Posters, branding, layouts and visual identity.',teacherIds:[]},
{id:'s12',name:'AI Tools',icon:'✨',category:'Technology',description:'Use modern AI tools creatively and responsibly.',teacherIds:[]}
],masters:[{"id":"photoshop-essentials","youtubeId":"um0UjpVudxE","title":"Photoshop Essentials","tool":"Photoshop","category":"design","icon":"🎨","instructor":"Ms. Devika Rao","difficulty":"Beginner","duration":"5h 20m","rating":4.9,"studentsCount":812,"description":"Go from a blank canvas to a finished poster. Learn layers, selections, retouching and typography the way working designers actually use them.","whatYoullLearn":["Navigate layers, masks and selections with confidence","Remove and replace backgrounds cleanly","Retouch photos without making them look fake","Design a poster with strong typography and color"],"lessons":[{"id":"l1","title":"Photoshop Basics","duration":"18m","type":"video"},{"id":"l2","title":"Layers","duration":"22m","type":"video"},{"id":"l3","title":"Selections & Masks","duration":"26m","type":"video"},{"id":"l4","title":"Background Removal","duration":"20m","type":"challenge","challenge":{"title":"Remove the background from a portrait","description":"Use the Selection and Mask tools to cleanly isolate a subject from its background."}},{"id":"l5","title":"Photo Retouching","duration":"24m","type":"video"},{"id":"l6","title":"Typography","duration":"19m","type":"video"},{"id":"l7","title":"Color & Lighting","duration":"21m","type":"video"},{"id":"l8","title":"Posters","duration":"30m","type":"challenge","challenge":{"title":"Design an event poster","description":"Combine an image, typography and color theory into a single poster layout."}},{"id":"l9","title":"Advanced Effects","duration":"23m","type":"video"},{"id":"l10","title":"Final Project","duration":"35m","type":"challenge","challenge":{"title":"Remove the background from this image and create a poster","description":"Bring together everything from the course into one finished, portfolio-ready poster."}}]},{"id":"figma-for-beginners","youtubeId":"HoKD1qIcchQ","title":"Figma for Beginners","tool":"Figma","category":"design","icon":"🖌️","instructor":"Mr. Owen Blake","difficulty":"Beginner","duration":"3h 10m","rating":4.8,"studentsCount":431,"description":"Design your first app screen and learn how modern design teams actually work together.","whatYoullLearn":["Frames, components and auto layout","Building a simple UI kit","Prototyping a working click-through demo"],"lessons":[{"id":"l1","title":"Interface Tour","duration":"12m","type":"video"},{"id":"l2","title":"Frames & Auto Layout","duration":"20m","type":"video"},{"id":"l3","title":"Components","duration":"18m","type":"video"},{"id":"l4","title":"Prototyping","duration":"22m","type":"challenge","challenge":{"title":"Prototype a login screen","description":"Build a two-screen clickable prototype for a simple login flow."}}]},{"id":"premiere-pro-basics","youtubeId":"xDq3ij-oHJA","title":"Premiere Pro Basics","tool":"Premiere Pro","category":"video","icon":"🎬","instructor":"Ms. Leah Fontaine","difficulty":"Beginner","duration":"4h 05m","rating":4.7,"studentsCount":356,"description":"Cut, pace and polish your first short video from raw footage to final export.","whatYoullLearn":["The timeline and cutting workflow","Transitions and pacing","Basic color correction and audio mixing"],"lessons":[{"id":"l1","title":"Importing & Organizing Footage","duration":"15m","type":"video"},{"id":"l2","title":"Cutting on the Timeline","duration":"24m","type":"video"},{"id":"l3","title":"Transitions & Pacing","duration":"20m","type":"video"},{"id":"l4","title":"Color & Audio","duration":"22m","type":"video"},{"id":"l5","title":"Export Settings","duration":"10m","type":"challenge","challenge":{"title":"Edit a 30-second reel","description":"Cut down provided clips into a tight, well-paced 30-second video."}}]},{"id":"capcut-for-shorts","youtubeId":"KYoCofR2TYY","title":"CapCut for Shorts","tool":"CapCut","category":"video","icon":"📱","instructor":"Mr. Diego Ruiz","difficulty":"Beginner","duration":"1h 45m","rating":4.6,"studentsCount":289,"description":"Fast, punchy edits for Reels and Shorts — captions, transitions and trending effects.","whatYoullLearn":["Auto-captions that actually look good","Beat-synced cuts","Templates vs. custom edits"],"lessons":[{"id":"l1","title":"CapCut Interface","duration":"10m","type":"video"},{"id":"l2","title":"Captions & Text","duration":"14m","type":"video"},{"id":"l3","title":"Beat Sync","duration":"16m","type":"challenge","challenge":{"title":"Beat-sync a 15 second clip","description":"Cut a short clip so the edits land on the beat of a track."}}]},{"id":"blender-3d-intro","youtubeId":"z-Xl9tGqH14","title":"Blender: Intro to 3D","tool":"Blender","category":"3d-game","icon":"🧊","instructor":"Ms. Priya Natarajan","difficulty":"Intermediate","duration":"6h 30m","rating":4.9,"studentsCount":274,"description":"Model, texture and render your first 3D object — from a simple mug to a stylized scene.","whatYoullLearn":["3D navigation and modeling basics","Materials, lighting and camera setup","Rendering your first scene"],"lessons":[{"id":"l1","title":"3D Navigation","duration":"16m","type":"video"},{"id":"l2","title":"Modeling Basics","duration":"28m","type":"video"},{"id":"l3","title":"Materials & Texturing","duration":"24m","type":"video"},{"id":"l4","title":"Lighting & Camera","duration":"20m","type":"video"},{"id":"l5","title":"Rendering","duration":"18m","type":"challenge","challenge":{"title":"Render a still-life scene","description":"Model, light and render three simple objects on a table."}}]},{"id":"unity-game-dev-101","youtubeId":"xw13Mlp25eE","title":"Unity Game Dev 101","tool":"Unity","category":"3d-game","icon":"🎮","instructor":"Mr. Felix Adeyemi","difficulty":"Intermediate","duration":"5h 50m","rating":4.7,"studentsCount":198,"description":"Build a simple 2D platformer from scratch and learn the game-loop mindset.","whatYoullLearn":["Scenes, GameObjects and components","Basic C# scripting for movement","Publishing a playable build"],"lessons":[{"id":"l1","title":"Unity Editor Tour","duration":"14m","type":"video"},{"id":"l2","title":"Player Movement Script","duration":"26m","type":"video"},{"id":"l3","title":"Collisions & Physics","duration":"22m","type":"video"},{"id":"l4","title":"Build a Level","duration":"30m","type":"challenge","challenge":{"title":"Build a 3-screen platformer level","description":"Assemble a small playable level using the assets provided."}}]},{"id":"excel-for-students","youtubeId":"Vl0H-qTclOg","title":"Excel for Students","tool":"Excel","category":"productivity","icon":"📊","instructor":"Ms. Grace Whitfield","difficulty":"Beginner","duration":"2h 15m","rating":4.8,"studentsCount":520,"description":"Formulas, formatting and pivot tables — the spreadsheet skills every club treasurer needs.","whatYoullLearn":["Core formulas (SUM, IF, VLOOKUP)","Formatting data that's easy to read","Building a simple budget with pivot tables"],"lessons":[{"id":"l1","title":"Formulas & Functions","duration":"20m","type":"video"},{"id":"l2","title":"Formatting & Charts","duration":"18m","type":"video"},{"id":"l3","title":"Pivot Tables","duration":"22m","type":"challenge","challenge":{"title":"Build a club budget tracker","description":"Turn a raw expense list into a summarized budget with a pivot table."}}]},{"id":"python-creative-coding","youtubeId":"eWRfhZUzrAc","title":"Python: Creative Coding","tool":"Python","category":"coding","icon":"🐍","instructor":"Mr. Arjun Mehta","difficulty":"Beginner","duration":"4h 40m","rating":4.9,"studentsCount":601,"description":"Learn Python by building things — a quiz game, a doodle generator and a mini data project.","whatYoullLearn":["Variables, loops and functions","Working with lists and simple data","Building a small interactive program"],"lessons":[{"id":"l1","title":"Python Basics","duration":"18m","type":"video"},{"id":"l2","title":"Loops & Conditionals","duration":"22m","type":"video"},{"id":"l3","title":"Functions","duration":"20m","type":"video"},{"id":"l4","title":"Build a Quiz Game","duration":"26m","type":"challenge","challenge":{"title":"Build a 5-question quiz game","description":"Use functions and conditionals to score a short multiple-choice quiz."}}]},{"id":"web-dev-html-css-js","youtubeId":"916GWv2Qs08","title":"Web Dev: HTML, CSS & JS","tool":"JavaScript","category":"coding","icon":"🧑‍💻","instructor":"Ms. Ilana Voss","difficulty":"Beginner","duration":"6h 00m","rating":4.8,"studentsCount":447,"description":"Build and style your first real webpage, then make it interactive with JavaScript.","whatYoullLearn":["Semantic HTML structure","Styling with CSS layout & flexbox","DOM interactivity with JavaScript"],"lessons":[{"id":"l1","title":"HTML Structure","duration":"20m","type":"video"},{"id":"l2","title":"CSS Styling & Layout","duration":"28m","type":"video"},{"id":"l3","title":"Flexbox","duration":"18m","type":"video"},{"id":"l4","title":"JavaScript Basics","duration":"24m","type":"video"},{"id":"l5","title":"Build a To-Do App","duration":"30m","type":"challenge","challenge":{"title":"Build a to-do list app","description":"Add, complete and delete items using vanilla JavaScript and the DOM."}}]},{"id":"photography-fundamentals","youtubeId":"lHcA7pPwYZY","title":"Photography Fundamentals","tool":"Photography","category":"photography","icon":"📸","instructor":"Ms. Ananya Rao","difficulty":"Beginner","duration":"3h 25m","rating":4.9,"studentsCount":388,"description":"Get off auto mode. Learn composition, light and exposure to take photos you're proud of.","whatYoullLearn":["Composition rules (and when to break them)","Understanding exposure triangle","Editing basics for a consistent look"],"lessons":[{"id":"l1","title":"Composition Basics","duration":"16m","type":"video"},{"id":"l2","title":"Understanding Exposure","duration":"20m","type":"video"},{"id":"l3","title":"Shooting in Manual Mode","duration":"22m","type":"challenge","challenge":{"title":"Shoot a 10-photo series","description":"Capture a themed 10-photo series using manual exposure settings."}}]}],projects:[],messages:[],protests:[]};
function load(){try{const raw=localStorage.getItem(KEY)||localStorage.getItem('skillswap_multipage_v3');const x=raw?JSON.parse(raw):null;return x?merge(defaults,x):structuredClone(defaults)}catch{return structuredClone(defaults)}}
function merge(a,b){const x=structuredClone(a);Object.keys(b||{}).forEach(k=>{x[k]=b[k]});return x}
let state=load();function save(){localStorage.setItem(KEY,JSON.stringify(state))}function id(p){return p+'-'+Math.random().toString(36).slice(2,9)}function esc(v=''){return String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}function initials(n='U'){return n.split(/\s+/).map(x=>x[0]).slice(0,2).join('').toUpperCase()}
function toast(msg){const t=document.getElementById('toast');if(!t)return;t.textContent=msg;t.classList.add('show');clearTimeout(window._tt);window._tt=setTimeout(()=>t.classList.remove('show'),2600)}
function current(){return state.current?state.users.find(u=>u.id===state.current.id)||null:null}
function requireLogin(next){if(!current()){toast('Please log in first.');location.href='/login';return}next()}
function page(){return document.body.dataset.page||'home'}
function nav(){const links=[['home','/','Home'],['dashboard','/dashboard','Dashboard'],['skills','/skills','Skills'],['masterclasses','/masterclasses','Masterclasses'],['tools','/tools','Tools'],['matches','/matches','Matches'],['projects','/projects','Projects'],['messages','/messages','Messages'],['community','/community','Community'],['protests','/protest-zone','Protest Zone'],['profile','/profile','Profile']];const nav=document.getElementById('mainNav');if(nav)nav.innerHTML=links.map(x=>`<a class="${page()===x[0]?'active':''}" href="${x[1]}">${x[2]}</a>`).join('');const a=document.getElementById('authArea');if(a){const u=current();a.innerHTML=u?`<a class="btn glass small" href="/profile">${esc(u.name)}</a><button class="btn small" id="logoutBtn">Log out</button>`:`<a class="btn glass small" href="/login">Log in</a><a class="btn primary small" href="/signup">Sign up</a>`;document.getElementById('logoutBtn')?.addEventListener('click',()=>{state.current=null;save();location.href='/'})}document.getElementById('menuBtn')?.addEventListener('click',()=>document.querySelector('.nav-inner').classList.toggle('open'))}
function layout(){nav();const y=document.getElementById('year');if(y)y.textContent=new Date().getFullYear()}
function skillCard(s){return `<article class="card"><div class="card-top"><div class="icon">${esc(s.icon||'✨')}</div><span class="tag">${esc(s.category||'Other')}</span></div><h3>${esc(s.name)}</h3><p>${esc(s.description||'Community skill.')}</p><div class="card-actions"><button class="btn glass" data-skill="${s.id}">Details</button><button class="btn primary" data-swap="${s.id}">Swap</button></div></article>`}
function renderSkills(){const grid=document.getElementById('skillGrid');if(!grid)return;const q=(document.getElementById('skillSearch')?.value||'').toLowerCase();const cat=document.getElementById('skillFilter')?.value||'all';const arr=state.skills.filter(s=>(!q||`${s.name} ${s.category} ${s.description}`.toLowerCase().includes(q))&&(cat==='all'||s.category===cat));grid.innerHTML=arr.length?arr.map(skillCard).join(''):`<div class="empty"><h3>No matching skills</h3><p>Add the first skill that is missing from the community.</p><a class="btn primary" href="/add-skill">＋ Add skill</a></div>`;grid.querySelectorAll('[data-skill]').forEach(b=>b.onclick=()=>detailSkill(b.dataset.skill));grid.querySelectorAll('[data-swap]').forEach(b=>b.onclick=()=>requireLogin(()=>toast('Swap request created for this skill. Connect the data adapter to a backend for real notifications.')))}
function setupSkills(){const sel=document.getElementById('skillFilter');if(sel)sel.innerHTML='<option value="all">All categories</option>'+[...new Set(state.skills.map(s=>s.category))].sort().map(c=>`<option>${esc(c)}</option>`).join('');document.getElementById('skillSearch')?.addEventListener('input',renderSkills);sel?.addEventListener('change',renderSkills);renderSkills()}
function detailSkill(sid){const s=state.skills.find(x=>x.id===sid);if(!s)return;const people=s.teacherIds.map(uid=>state.users.find(u=>u.id===uid)).filter(Boolean);openModal(`<div class="eyebrow">${esc(s.category)}</div><h2>${esc(s.icon)} ${esc(s.name)}</h2><p class="muted">${esc(s.description)}</p><h3>People offering this skill</h3>${people.length?people.map(u=>`<div class="person"><span class="avatar">${initials(u.name)}</span>${esc(u.name)}</div>`).join(''):'<p class="muted">No registered teacher is linked yet.</p>'}<br><button class="btn primary full" id="modalSwap">Request a swap</button>`);document.getElementById('modalSwap').onclick=()=>requireLogin(()=>{closeModal();toast('Swap request prepared.')})}
function openModal(html){document.getElementById('modalContent').innerHTML=html;document.getElementById('modal').classList.remove('hidden')}function closeModal(){document.getElementById('modal').classList.add('hidden')}
function renderMasters(){const g=document.getElementById('masterGrid');if(!g)return;g.innerHTML=state.masters.map((m)=>`<article class="card"><div class="card-top"><div class="icon">${esc(m.icon)}</div><span class="tag">${esc(m.difficulty)}</span></div><h3>${esc(m.title)}</h3><p>${esc(m.description||m.desc)}</p><div class="project-meta"><small class="muted">${esc(m.instructor||'SkillSwap')}</small><small class="muted">${m.lessons.length} lessons · ${esc(m.duration||'')}</small></div><div class="card-actions"><span class="tag">${esc(m.tool||'Learning')}</span><span class="tag">★ ${m.rating||'—'}</span></div><a class="btn primary full" href="/masterclass-detail?id=${encodeURIComponent(m.id)}">Open Masterclass →</a></article>`).join('')}

function setupMasterDetail(){const idv=new URLSearchParams(location.search).get('id');const m=state.masters.find(x=>x.id===idv)||state.masters[0];const root=document.getElementById('masterDetail');if(!root)return;if(!m){root.innerHTML='<div class="empty"><h3>Masterclass not found</h3><a class="btn primary" href="/masterclasses">Back to Masterclasses</a></div>';return}document.title=`${m.title} — SkillSwap`;root.innerHTML=`<div class="section-head"><div><div class="eyebrow">MASTERCLASS</div><h2>${esc(m.title)}</h2><p class="muted">${esc(m.description||m.desc)}</p></div><a class="btn glass" href="/masterclasses">← All Masterclasses</a></div><div class="card"><div class="card-top"><div class="icon">${esc(m.icon)}</div><span class="tag">${esc(m.difficulty)}</span></div><p class="muted">${esc(m.instructor||'SkillSwap instructor')} · ${esc(m.duration||'')} · ★ ${m.rating||'—'} · ${m.studentsCount||0} learners</p><div class="master-video"><iframe src="https://www.youtube-nocookie.com/embed/${encodeURIComponent(m.youtubeId||'')}" title="${esc(m.title)} embedded lesson" loading="lazy" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen></iframe></div><div class="section-head"><div><div class="eyebrow">WHAT YOU'LL LEARN</div><h3>Course outcomes</h3></div></div><div class="grid">${(m.whatYoullLearn||[]).map((x,i)=>`<div class="card"><div class="icon">${i+1}</div><p>${esc(x)}</p></div>`).join('')}</div><div class="section-head"><div><div class="eyebrow">LEARNING PATH</div><h3>${m.lessons.length} lessons</h3></div></div><div class="grid">${m.lessons.map((l,i)=>`<article class="card"><div class="card-top"><div class="icon">${l.type==='challenge'?'🎯':'▶'}</div><span class="tag">${esc(l.duration||'Lesson')}</span></div><h3>${esc(l.title)}</h3><p class="muted">${l.type==='challenge'?'Challenge':'Video lesson'}</p><a class="btn glass full" href="/lesson?master=${encodeURIComponent(m.id)}&lesson=${encodeURIComponent(l.id)}">Open lesson →</a></article>`).join('')}</div></div>`}

function setupLesson(){const mid=new URLSearchParams(location.search).get('master');const lid=new URLSearchParams(location.search).get('lesson');const m=state.masters.find(x=>x.id===mid)||state.masters[0];const root=document.getElementById('lessonContent');if(!root||!m)return;const idx=Math.max(0,m.lessons.findIndex(x=>x.id===lid));const lesson=m.lessons[idx];document.title=`${lesson.title} — ${m.title} — SkillSwap`;const prev=m.lessons[idx-1],next=m.lessons[idx+1];root.innerHTML=`<div class="section-head"><div><a class="btn glass" href="/masterclass-detail?id=${encodeURIComponent(m.id)}">← Back to Masterclass</a><div class="eyebrow" style="margin-top:18px">LESSON ${idx+1} OF ${m.lessons.length}</div><h2>${esc(lesson.title)}</h2><p class="muted">${esc(m.title)} · ${esc(lesson.duration||'')}</p></div></div><div class="master-video"><iframe src="https://www.youtube-nocookie.com/embed/${encodeURIComponent(lesson.youtubeId||m.youtubeId||'')}" title="${esc(lesson.title)} video lesson" loading="lazy" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen></iframe></div><div class="form-card"><div class="eyebrow">${lesson.type==='challenge'?'PRACTICE CHALLENGE':'STEP-BY-STEP'}</div><h3>${esc(lesson.challenge?.title||lesson.title)}</h3><p class="muted">${esc(lesson.challenge?.description||'Watch the embedded lesson, follow along in your own project, and try the technique once before moving on.')}</p><div class="pill-row">${['Watch the lesson','Follow along','Try it yourself'].map((x,i)=>`<span class="tag">${i+1}. ${x}</span>`).join('')}</div></div><div class="button-row"><a class="btn glass ${prev?'':'hidden'}" href="${prev?`/lesson?master=${encodeURIComponent(m.id)}&lesson=${encodeURIComponent(prev.id)}`:'#'}">← Previous</a><a class="btn primary ${next?'':'hidden'}" href="${next?`/lesson?master=${encodeURIComponent(m.id)}&lesson=${encodeURIComponent(next.id)}`:'#'}">Next lesson →</a>${!next?`<a class="btn primary" href="/certificate?master=${encodeURIComponent(m.id)}">View completion page →</a>`:''}</div>`}

function renderTools(){const g=document.getElementById('toolGrid');if(!g)return;const tools=[['🌿','Git','Developer Tools','Version control basics — commit, branch, merge, push.'],['🧩','JSON Formatter','Developer Tools','Paste messy JSON and get clean, indented output.'],['🔎','Regex Tester','Developer Tools','Test regular expressions against sample text.'],['🎨','Color Palette Generator','Creative Tools','Generate a matching palette from one base color.'],['✂️','Background Remover','Creative Tools','Plan a clean background-removal workflow.'],['🧮','Calculator','Study Tools','Standard calculations right in your browser.'],['📏','Unit Converter','Study Tools','Convert common units quickly.'],['❓','Quiz Generator','Study Tools','Turn notes into a quick multiple-choice quiz.'],['🗂️','Flashcards','Study Tools','Build and review flashcard decks.'],['📄','PDF Tools','Productivity Tools','Useful workflows for merging and splitting PDFs.']];g.innerHTML=tools.map(t=>`<article class="card"><div class="card-top"><div class="icon">${t[0]}</div><span class="tag">${t[2]}</span></div><h3>${t[1]}</h3><p>${t[3]}</p><button class="btn glass full" data-tool="${esc(t[1])}">Open tool →</button></article>`).join('');g.querySelectorAll('[data-tool]').forEach(b=>b.onclick=()=>openModal(`<div class="eyebrow">TOOL</div><h2>${esc(b.dataset.tool)}</h2><p class="muted">This tool space is ready for an interactive implementation without changing the SkillSwap visual system.</p>`))}

function renderMatches(){const g=document.getElementById('matchGrid');if(!g)return;const me=current();if(!me){g.innerHTML='<div class="empty"><h3>Log in to see skill matches</h3><p>Matches are calculated from real registered user profiles.</p><a class="btn primary" href="/login">Log in</a></div>';return}const my=state.profiles[me.id]||{teach:[],learn:[]};const learn=(my.learn||[]).map(x=>x.toLowerCase());const teach=(my.teach||[]).map(x=>x.toLowerCase());const candidates=state.users.filter(u=>u.id!==me.id).map(u=>{const p=state.profiles[u.id]||{};const theirTeach=p.teach||[];const theirLearn=p.learn||[];const score=theirTeach.filter(x=>learn.includes(x.toLowerCase())).length*50+theirLearn.filter(x=>teach.includes(x.toLowerCase())).length*50;return {u,p,score}}).filter(x=>x.score>0).sort((a,b)=>b.score-a.score);g.innerHTML=candidates.length?candidates.map(x=>`<article class="card"><div class="card-top"><div class="avatar">${initials(x.u.name)}</div><span class="tag">${x.score}% match</span></div><h3>${esc(x.u.name)}</h3><p class="muted">Teaches: ${(x.p.teach||[]).map(esc).join(', ')||'Not added'}</p><p class="muted">Wants: ${(x.p.learn||[]).map(esc).join(', ')||'Not added'}</p><a class="btn primary full" href="/profile?id=${encodeURIComponent(x.u.id)}">View profile →</a></article>`).join(''):'<div class="empty"><h3>No matches yet</h3><p>Add skills you teach and want to learn in Settings, then check again.</p><a class="btn primary" href="/settings">Update profile</a></div>'}

function renderCommunity(){const g=document.getElementById('communityGrid');if(!g)return;const users=state.users;g.innerHTML=users.length?users.map(u=>{const p=state.profiles[u.id]||{};return `<article class="card"><div class="card-top"><div class="avatar">${initials(u.name)}</div><span class="tag">Member</span></div><h3>${esc(u.name)}</h3><p>${esc(p.bio||'No bio added yet.')}</p><p class="muted">Teaches: ${(p.teach||[]).map(esc).join(', ')||'Not added'}</p><p class="muted">Learns: ${(p.learn||[]).map(esc).join(', ')||'Not added'}</p><a class="btn glass full" href="/profile?id=${encodeURIComponent(u.id)}">View profile →</a></article>`}).join(''):'<div class="empty"><h3>No registered users yet</h3><p>The community appears here as real people create SkillSwap accounts.</p><a class="btn primary" href="/signup">Create an account</a></div>'}

function renderDashboard(){const root=document.getElementById('dashboardContent');if(!root)return;const me=current();if(!me){root.innerHTML='<div class="empty"><h3>Welcome to SkillSwap</h3><p>Log in to see your dashboard, learning path and matches.</p><a class="btn primary" href="/login">Log in</a></div>';return}const p=state.profiles[me.id]||{teach:[],learn:[]};root.innerHTML=`<div class="section-head"><div><div class="eyebrow">YOUR DASHBOARD</div><h2>Welcome back, ${esc(me.name)}.</h2><p class="muted">Keep learning, teaching and swapping.</p></div><a class="btn glass" href="/profile">View profile</a></div><div class="grid"><div class="card"><div class="icon">↔</div><h3>Skills you teach</h3><p>${(p.teach||[]).map(esc).join(', ')||'Add your first teaching skill.'}</p></div><div class="card"><div class="icon">＋</div><h3>Skills you want</h3><p>${(p.learn||[]).map(esc).join(', ')||'Add learning goals in Settings.'}</p></div><div class="card"><div class="icon">🎓</div><h3>Masterclasses</h3><p>${state.masters.length} structured courses are available.</p><a class="btn glass" href="/masterclasses">Browse classes</a></div></div><div class="section-head"><div><div class="eyebrow">NEXT STEP</div><h3>Build your profile</h3></div><a class="btn primary" href="/settings">Edit settings →</a></div>`}

function setupCertificate(){const m=state.masters.find(x=>x.id===new URLSearchParams(location.search).get('master'))||state.masters[0];const root=document.getElementById('certificateContent');if(!root)return;const me=current();root.innerHTML=`<div class="form-card"><div class="eyebrow">COMPLETION</div><h2>Masterclass Complete</h2><p class="muted">${esc(m.title)}</p><div class="card"><div class="icon">🏆</div><h3>${esc(me?.name||'SkillSwap learner')}</h3><p class="muted">Completed learning path: ${esc(m.title)}</p></div><div class="button-row"><a class="btn primary" href="/masterclasses">Explore more classes</a><a class="btn glass" href="/profile">Back to profile</a></div></div>`}

function renderProjects(){const g=document.getElementById('projectGrid');if(!g)return;g.innerHTML=state.projects.length?state.projects.map(p=>{const u=state.users.find(x=>x.id===p.creatorId);return `<article class="card"><div class="cover">${p.cover?`<img src="${p.cover}" alt="Project preview">`:`${esc(p.icon||'🚀')}`}</div><div class="project-meta"><span class="tag">${esc(p.category)}</span><small class="muted">${new Date(p.createdAt).toLocaleDateString()}</small></div><h3>${esc(p.title)}</h3><p>${esc(p.description)}</p><div class="tools">${(p.tools||[]).map(t=>`<span class="tag">${esc(t)}</span>`).join('')}</div><div class="creator"><span class="avatar">${initials(u?.name||'U')}</span><span>Created by <b>${esc(u?.name||'Unknown user')}</b></span></div></article>`}).join(''):`<div class="empty"><h3>No projects shared yet</h3><p>Registered users can publish the first project.</p><a class="btn primary" href="/upload-project">＋ Upload project</a></div>`}
function renderUsers(){const list=document.getElementById('userList');if(!list)return;const users=state.users;list.innerHTML=`<div class="user-item selected" data-user="community"><span class="avatar">🌐</span>Community</div>`+(users.length?users.map(u=>`<div class="user-item" data-user="${u.id}"><span class="avatar">${initials(u.name)}</span>${esc(u.name)}</div>`).join(''):'<div class="muted small" style="padding:12px">No user registered yet</div>');list.querySelectorAll('[data-user]').forEach(b=>b.onclick=()=>selectChat(b.dataset.user))}
let selectedChat='community';function selectChat(uid){selectedChat=uid;document.querySelectorAll('[data-user]').forEach(x=>x.classList.toggle('selected',x.dataset.user===uid));const h=document.getElementById('chatHeading'),s=document.getElementById('chatSubheading');if(uid==='community'){h.textContent='Community';s.textContent='Global room'}else{const u=state.users.find(x=>x.id===uid);h.textContent=u?.name||'User';s.textContent='Direct conversation'}renderMessages()}
function chatKey(a,b){return[a,b].sort().join('|')}function renderMessages(){const box=document.getElementById('chatMessages');if(!box)return;const me=current();let msgs;if(selectedChat==='community')msgs=state.messages.filter(m=>m.room==='community');else if(me)msgs=state.messages.filter(m=>m.room===chatKey(me.id,selectedChat));else msgs=[];box.innerHTML=msgs.length?msgs.map(m=>{const u=state.users.find(x=>x.id===m.senderId);return `<div class="bubble ${me&&m.senderId===me.id?'mine':''}">${esc(m.text)}<small>${esc(u?.name||'User')} · ${new Date(m.time).toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'})}</small></div>`}).join(''):`<div class="empty"><h3>No messages yet</h3><p>Start the conversation.</p></div>`;box.scrollTop=box.scrollHeight}
function setupMessages(){renderUsers();selectChat('community');const form=document.getElementById('composer');form?.addEventListener('submit',e=>{e.preventDefault();const me=current();if(!me)return requireLogin(()=>{});const input=document.getElementById('messageInput'),text=input.value.trim();if(!text)return;state.messages.push({id:id('msg'),room:selectedChat==='community'?'community':chatKey(me.id,selectedChat),senderId:me.id,text,time:Date.now()});save();input.value='';renderMessages()})}
function renderZones(){const g=document.getElementById('zoneGrid');if(!g)return;document.getElementById('statZones')?.textContent&&(document.getElementById('statZones').textContent=state.protests.length);g.innerHTML=state.protests.length?state.protests.map(z=>`<article class="zone-card"><div class="live">● LIVE DISCUSSION</div><h3>${esc(z.title)}</h3><p class="muted">${esc(z.description)}</p><div class="room-meta"><span>👥 ${z.participants.length}</span><span>Host: ${esc(state.users.find(u=>u.id===z.hostId)?.name||'Unknown')}</span></div><a class="btn primary full" href="/protest?id=${encodeURIComponent(z.id)}">Open room →</a></article>`).join(''):`<div class="empty"><h3>No community zones yet</h3><p>Create a peaceful discussion room around an issue or idea.</p><a class="btn primary" href="/protest-zone#create">＋ Start a zone</a></div>`}
function setupRoom(){const z=state.protests.find(x=>x.id===new URLSearchParams(location.search).get('id'));if(!z){document.getElementById('roomContent').innerHTML='<div class="empty"><h3>Room not found</h3><a class="btn primary" href="/protest-zone">Back to Protest Zone</a></div>';return}document.getElementById('roomTitle').textContent=z.title;document.getElementById('roomDesc').textContent=z.description;document.getElementById('roomHost').textContent=state.users.find(u=>u.id===z.hostId)?.name||'Unknown';document.getElementById('roomCount').textContent=z.participants.length;const me=current();const joined=!!me&&z.participants.includes(me.id);const jb=document.getElementById('joinRoomBtn');jb.textContent=joined?'Leave zone':'Join zone';jb.onclick=()=>{if(!current())return requireLogin(()=>{});const i=z.participants.indexOf(current().id);if(i>=0)z.participants.splice(i,1);else z.participants.push(current().id);save();setupRoom()};const box=document.getElementById('roomMessages');const msgs=state.messages.filter(m=>m.room==='protest:'+z.id);box.innerHTML=msgs.length?msgs.map(m=>`<div class="bubble ${me&&m.senderId===me.id?'mine':''}">${esc(m.text)}<small>${esc(state.users.find(u=>u.id===m.senderId)?.name||'User')}</small></div>`).join(''):'<div class="empty"><h3>No discussion yet</h3><p>Join the room and share a respectful view.</p></div>';box.scrollTop=box.scrollHeight;document.getElementById('roomComposer')?.addEventListener('submit',sendRoomMessage,{once:true})}
function sendRoomMessage(e){e.preventDefault();const me=current();if(!me)return requireLogin(()=>{});const z=state.protests.find(x=>x.id===new URLSearchParams(location.search).get('id'));if(!z.participants.includes(me.id))return toast('Join the zone before posting.');const input=document.getElementById('roomInput'),text=input.value.trim();if(!text)return;state.messages.push({id:id('msg'),room:'protest:'+z.id,senderId:me.id,text,time:Date.now()});save();setupRoom()}
function setupHome(){document.getElementById('statUsers')?.textContent&&(document.getElementById('statUsers').textContent=state.users.length);document.getElementById('statSkills')?.textContent&&(document.getElementById('statSkills').textContent=state.skills.length);document.getElementById('statProjects')?.textContent&&(document.getElementById('statProjects').textContent=state.projects.length);document.getElementById('statZones')?.textContent&&(document.getElementById('statZones').textContent=state.protests.length);document.getElementById('heroJoinBtn')?.addEventListener('click',()=>location.href=current()?'/profile':'/signup')}
function setupSignup(){document.getElementById('signupForm')?.addEventListener('submit',e=>{e.preventDefault();const name=document.getElementById('name').value.trim(),username=(document.getElementById('username')?.value||'').trim(),email=document.getElementById('email').value.trim().toLowerCase(),password=document.getElementById('password').value,teach=(document.getElementById('teach')?.value||'').split(',').map(x=>x.trim()).filter(Boolean),learn=(document.getElementById('learn')?.value||'').split(',').map(x=>x.trim()).filter(Boolean);if(!name||!email||!password)return toast('Complete all fields.');if(state.users.some(u=>u.email===email))return toast('An account with that email already exists.');if(username&&state.users.some(u=>(u.username||'').toLowerCase()===username.toLowerCase()))return toast('That username is already in use.');const u={id:id('user'),name,username,email,createdAt:Date.now()};state.users.push(u);state.current={id:u.id};state.profiles[u.id]={bio:'',teach,learn};save();location.href='/profile'})}
function setupLogin(){document.getElementById('loginForm')?.addEventListener('submit',e=>{e.preventDefault();const email=document.getElementById('email').value.trim().toLowerCase();const u=state.users.find(x=>x.email===email);if(!u)return toast('No user registered with that email.');state.current={id:u.id};save();location.href='/'})}
function setupAddSkill(){document.getElementById('addSkillForm')?.addEventListener('submit',e=>{e.preventDefault();requireLogin(()=>{const name=document.getElementById('name').value.trim(),cat=document.getElementById('category').value.trim()||'Other',icon=document.getElementById('icon').value.trim()||'✨',desc=document.getElementById('description').value.trim();if(!name||!desc)return toast('Add a name and description.');state.skills.unshift({id:id('skill'),name,category:cat,icon,description:desc,teacherIds:[current().id]});state.profiles[current().id].teach=[...(state.profiles[current().id].teach||[]),name];save();location.href='/skills'})})}
function setupUpload(){document.getElementById('uploadForm')?.addEventListener('submit',e=>{e.preventDefault();requireLogin(()=>{const title=document.getElementById('title').value.trim(),cat=document.getElementById('category').value.trim()||'Other',icon=document.getElementById('icon').value.trim()||'🚀',desc=document.getElementById('description').value.trim(),tools=document.getElementById('tools').value.split(',').map(x=>x.trim()).filter(Boolean),file=document.getElementById('image').files[0];if(!title||!desc)return toast('Add a title and description.');const done=cover=>{state.projects.unshift({id:id('project'),title,category:cat,icon,description:desc,tools,cover,creatorId:current().id,createdAt:Date.now()});save();location.href='/projects'};if(file){if(file.size>1200000)return toast('Preview image must be under 1.2 MB.');const r=new FileReader();r.onload=()=>done(r.result);r.readAsDataURL(file)}else done('')})})}
function setupProfile(){const g=document.getElementById('profileContent');if(!g)return;const qid=new URLSearchParams(location.search).get('id');const u=qid?state.users.find(x=>x.id===qid):current();if(!u){g.innerHTML='<div class="empty"><h3>No user logged in</h3><p>Log in or create an account to view your profile.</p><a class="btn primary" href="/login">Log in</a></div>';return}const p=state.profiles[u.id]||{bio:'',teach:[],learn:[]};const own=!qid||current()?.id===u.id;g.innerHTML=`<div class="profile-card"><div class="profile-avatar">${initials(u.name)}</div><div><div class="eyebrow">${own?'YOUR PROFILE':'COMMUNITY PROFILE'}</div><h2>${esc(u.name)}</h2><p class="muted">${esc(p.bio||'No bio added yet.')}</p><h3>Teach</h3><div class="pill-row">${(p.teach||[]).map(x=>`<span class="tag">${esc(x)}</span>`).join('')||'<span class="muted">No skills added yet.</span>'}</div><h3 style="margin-top:18px">Want to learn</h3><div class="pill-row">${(p.learn||[]).map(x=>`<span class="tag">${esc(x)}</span>`).join('')||'<span class="muted">No learning goals yet.</span>'}</div><div class="button-row" style="margin-top:22px">${own?'<a class="btn primary" href="/settings">Edit profile</a><a class="btn glass" href="/add-skill">＋ Add skill</a>':'<a class="btn primary" href="/messages">Message</a>'}</div></div></div>`}
function setupSettings(){const u=current();if(!u)return location.href='/login';const p=state.profiles[u.id]||{bio:'',teach:[],learn:[]};document.getElementById('name').value=u.name;document.getElementById('email').value=u.email;document.getElementById('bio').value=p.bio||'';document.getElementById('teach').value=(p.teach||[]).join(', ');document.getElementById('learn').value=(p.learn||[]).join(', ');document.getElementById('settingsForm').onsubmit=e=>{e.preventDefault();u.name=document.getElementById('name').value.trim()||u.name;state.profiles[u.id]={bio:document.getElementById('bio').value.trim(),teach:document.getElementById('teach').value.split(',').map(x=>x.trim()).filter(Boolean),learn:document.getElementById('learn').value.split(',').map(x=>x.trim()).filter(Boolean)};save();toast('Profile updated.');setTimeout(()=>location.href='/profile',500)}}
function setupCreateZone(){document.getElementById('zoneForm')?.addEventListener('submit',e=>{e.preventDefault();requireLogin(()=>{const title=document.getElementById('title').value.trim(),desc=document.getElementById('description').value.trim();if(!title||!desc)return toast('Add a title and description.');state.protests.unshift({id:id('zone'),title,description:desc,hostId:current().id,participants:[current().id],createdAt:Date.now()});save();location.href='/protest-zone'})})}
function common(){document.getElementById('closeModal')?.addEventListener('click',closeModal);document.getElementById('modal')?.addEventListener('click',e=>{if(e.target.id==='modal')closeModal()});document.querySelectorAll('[data-year]').forEach(x=>x.textContent=new Date().getFullYear())}
export function initApp(){layout();common();const p=page();({home:setupHome,skills:setupSkills,masterclasses:renderMasters,masterdetail:setupMasterDetail,lesson:setupLesson,tools:renderTools,matches:renderMatches,community:renderCommunity,dashboard:renderDashboard,certificate:setupCertificate,projects:renderProjects,messages:setupMessages,protests:()=>{renderZones();setupCreateZone()},room:setupRoom,signup:setupSignup,login:setupLogin,addskill:setupAddSkill,upload:setupUpload,profile:setupProfile,settings:setupSettings}[p]||(()=>{}))()}
window.addEventListener('storage',()=>{state=load();nav()});

